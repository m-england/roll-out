import { JsonEditorDirective } from './json-editor.directive';

describe('JsonEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonEditorDirective();
    expect(directive).toBeTruthy();
  });
});
